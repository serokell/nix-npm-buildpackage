{
  "name": "simple-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p out && webpack --version >> out/result"
  },
  "files": [
    "out"
  ],
  "dependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
